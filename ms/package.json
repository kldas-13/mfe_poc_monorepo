{
  "name": "meta-shell",
  "version": "1.0.0",
  "description": "Unified Canvas Meta Shell — qiankun orchestrator. Dashboard, Overview, and full-screen canvas pages.",
  "type": "module",
  "scripts": {
    "dev":     "vite --port 5173 --host",
    "build":   "vite build",
    "preview": "vite preview --port 5173 --host"
  },
  "dependencies": {
    "qiankun": "^2.10.16"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite":       "^5.1.3"
  },
  "engines": { "node": ">=18" }
}
